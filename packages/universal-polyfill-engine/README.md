# `universal-polyfill-engine`

> Set polyfill engine

## Usage

```js
import Engine from 'universal-polyfill-engine';

import React from 'react';
Engine.set(React);

import Rax from 'rax';
Engine.set(Rax);
```
